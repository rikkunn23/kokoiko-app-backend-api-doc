name: Auto Assign PR

on:
  pull_request:
    types: [opened]

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - name: Assign PR to author
        uses: pozil/auto-assign-issue@v1
        with:
          assignees: ${{ github.actor }}
